# Usage

migrations are used to track changes to the db via tasks. The migrations are executed on production.

### To create a new migration, run the following command:
```pascal
./make-migration "description"
```
### Example
```pascal
./make-migration "add phone filed in order checkout"
```
